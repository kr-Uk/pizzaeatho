<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.pizza.model.dao.CommentDao">

    <insert id="insert" parameterType="Comment" useGeneratedKeys="true" keyProperty="commentId">
        INSERT INTO `COMMENT` (user_id, product_id, order_detail_id, rating, comment)
        VALUES (#{userId}, #{productId}, #{orderDetailId}, #{rating}, #{comment})
    </insert>

    <select id="selectByProduct" resultType="CommentInfo">
        SELECT
            u.name AS user_name,
            c.rating,
            c.comment,
            DATE_FORMAT(c.created_at, '%Y-%m-%d') AS created_at
        FROM `COMMENT` c
        JOIN `USER` u ON c.user_id = u.user_id
        WHERE c.product_id = #{productId}
        ORDER BY c.created_at DESC
    </select>
</mapper>

