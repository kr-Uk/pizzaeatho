<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.pizza.model.dao.UserDao">

    <select id="selectByCredentials" resultType="User">
        SELECT user_id, id, pw, name, stamp
        FROM `USER`
        WHERE id = #{id} AND pw = #{pw}
    </select>

    <select id="selectByUserId" resultType="User">
        SELECT user_id, id, pw, name, stamp
        FROM `USER`
        WHERE user_id = #{userId}
    </select>

    <select id="countById" resultType="int">
        SELECT COUNT(*)
        FROM `USER`
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO `USER` (id, pw, name, stamp)
        VALUES (#{id}, #{pw}, #{name}, #{stamp})
    </insert>

    <update id="addStamp">
        UPDATE `USER`
        SET stamp = stamp + #{count}
        WHERE user_id = #{userId}
    </update>
</mapper>

