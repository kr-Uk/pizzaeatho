<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.pizza.model.dao.OrderDetailDao">

    <insert id="insertOrderDetail" parameterType="OrderDetail" useGeneratedKeys="true" keyProperty="orderDetailId">
        INSERT INTO ORDER_DETAIL (order_id, product_id, dough_id, crust_id, quantity, unit_price)
        VALUES (#{orderId}, #{productId}, #{doughId}, #{crustId}, #{quantity}, #{unitPrice})
    </insert>

    <insert id="insertOrderDetailTopping" parameterType="OrderDetailTopping">
        INSERT INTO ORDER_DETAIL_TOPPING (order_detail_id, topping_id, quantity)
        VALUES (#{orderDetailId}, #{toppingId}, #{quantity})
    </insert>
</mapper>

